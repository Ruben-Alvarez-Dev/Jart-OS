# Jart-OS Study Domain Agent
# Spec: §18 "Study Domain — 5 Blocks"
#       §15 "Domain Map"

services:
  study_domain:
    build:
      context: ../../..
      dockerfile: study/Dockerfile.study_domain
    container_name: jart-os-study-domain
    environment:
      - AGENT_ROLE=study_domain
      - AGENT_PORT=10500
      - NATS_URL=nats://token:${NATS_TOKEN:?NATS_TOKEN env var is required}@nats:4222
      - REDIS_URL=redis://:${REDIS_PASSWORD:?REDIS_PASSWORD env var is required}@redis:6379
      - LITELLM_URL=http://litellm:4000
      - LITELLM_KEY=${LITELLM_KEY:?LITELLM_KEY env var is required}
    ports:
      - "10500:10500"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config
      - ./topics:/app/topics
      - ./exams:/app/exams
      - ./protocols:/app/protocols
    restart: unless-stopped
    networks: [jart-os-net]
    depends_on:
      - nats
      - redis

networks:
  jart-os-net:
    external: true
